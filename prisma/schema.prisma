// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native"]
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id          String       @id @default(cuid())
  email       String       @unique
  name        String?
  password    String?
  role        String       @default("user") // "admin" or "user"
  createdAt   String       @default("2024-01-01T00:00:00.000Z")

  // Add more fields as needed for gym app
  workouts    Workout[]
  attendances Attendance[]
}

model Workout {
  id          String   @id @default(cuid())
  userId      String
  name        String
  description String?
  exercises   Exercise[]

  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model Exercise {
  id          String   @id @default(cuid())
  workoutId   String
  name        String
  sets        Int?
  reps        Int?
  weight      Float?

  workout     Workout  @relation(fields: [workoutId], references: [id], onDelete: Cascade)
}

model Attendance {
  id        String   @id @default(cuid())
  userId    String
  date      String   // Formato: YYYY-MM-DD
  timestamp String   @default("2024-01-01T00:00:00.000Z")
  createdAt String   @default("2024-01-01T00:00:00.000Z")

  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, date]) // Un usuario solo puede tener una asistencia por día
}